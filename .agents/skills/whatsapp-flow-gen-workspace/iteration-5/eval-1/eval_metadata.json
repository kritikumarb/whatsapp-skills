{
  "eval_id": 1,
  "eval_name": "flight-booking-data-exchange",
  "prompt": "Create a two-screen flow for a flight booking. Screen 1: select destination, Screen 2: select seat. Screen 1 should use data-exchange to fetch available seats for the selected destination. Provide flow.json and Python backend.",
  "assertions": [
    "The generated flow.json uses 'data_exchange' actions.",
    "The 'data_exchange' payload includes the 'data_exchange_trigger' attribute.",
    "The backend logic uses 'data_exchange_trigger' to identify the action.",
    "The backend handles 'ping', 'data_exchange', and 'complete' actions."
  ]
}